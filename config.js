const config = {
    socket: {
        actions: {
            CREATE_POST: "createPost",
            GET_TAGS: "getTags",
            UPDATE_POST: "updatePost",
            DELETE_POST: "deletePost",
        }
    }
}

export default config;